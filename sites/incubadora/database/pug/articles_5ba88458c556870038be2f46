extends incuLayoutAdmin.pug
block content
	h1 ARTICLES

	h2 ACTIONS
	a(href="/admin/article/-1") CREATE
	h2 AVAILABLE
	ul.list-group#simpleList
		each item,index in articles
			li.list-group-item(data-name=item.name)
				.row.no-gutters
					.col-12
						a(href="/admin/article/"+item.name)!=item.title
						span=item.type
block scripts
	script(src="https://cdn.jsdelivr.net/npm/sortablejs@1.6.1/Sortable.min.js")
	script.
		var el = document.querySelector('#simpleList')
		Sortable.create(el, { 
			onSort: function (/**Event*/evt) {
				console.log($('li').length);
			}
		});
		//let r = await misitiobaApi.save('articles', payload, options)