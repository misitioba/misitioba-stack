module.exports = app => {
	
	app.srv.misitiobaApi.configure('incuba')
	
	app.get('/admin', (req,res)=>{
		res.sendView('admin')
	});
	app.get('/admin/login', (req,res)=>{
		res.sendView('adminLogin')
	});
	
}